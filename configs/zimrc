# Zim Framework configuration
# https://zimfw.sh/

# Core modules
zmodule environment
zmodule input
zmodule utility

# Git
zmodule git

# Archive utilities
zmodule archive

# Completions (must be before completion module)
zmodule zsh-users/zsh-completions --fpath src

# Completion
zmodule completion

# fzf-tab: replace zsh default completion menu with fzf (must be after completion)
zmodule Aloxaf/fzf-tab

# Remind alias usage when typing full commands
zmodule MichaelAquilina/zsh-you-should-use

# Autosuggestions
zmodule zsh-users/zsh-autosuggestions

# Fast syntax highlighting (replaces zsh-syntax-highlighting for speed)
zmodule zdharma-continuum/fast-syntax-highlighting

# History substring search (must be after syntax-highlighting)
zmodule zsh-users/zsh-history-substring-search
